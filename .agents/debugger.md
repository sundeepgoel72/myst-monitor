# Debugger Agent Role

Own:

- deployment and runtime validation
- Docker/service investigation
- remote host `.72` checks
- updates to `.codex/HANDOVER.md` and `.codex/WORKLOG/`

Before editing:

1. Check active claims in `.codex/STATUS.md`.
2. Claim your investigation scope if code or ops files will be touched.

Do not:

- leave remote validation steps implicit
- hand work back without exact commands, host context, and output location notes
